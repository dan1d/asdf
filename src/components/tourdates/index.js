import { connect } from 'react-redux';
import Tourdates from './view';


const select = (state, props) => ({
});

const perform = dispatch => ({
});

export default connect(
  select,
  perform
)(Tourdates);
